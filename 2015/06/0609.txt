＠linuxでデータベースを読み込めない件の調査
・問題個所の特定
　－キーポイントではなく，特徴量の読み込みが一致していないため計算結果でエラー
　　→windowsでは一致している
　－データベース作成時点では，windowsとLinuxで違いは見られない
　　※特徴量の数値が1異なる場合もあるが問題なし？

A.linuxで作成したデータベースをlinuxでロード		×
B.windowsで作成したデータベースをwindowsでロード	○

→A≠B
C.linuxで作成したデータベースをwindowsでロード		○
D.windowsで作成したデータベースをwindowsでロード	○
→C＝D

E.windowsで作成したデータベースをlinuxでロード		×
F.windwosで作成したデータベースをwindowsでロード	○
→E≠F

⇒linuxにおけるnp.loadの挙動がおかしい

☆原因
　glab.glabでファイル名のリストを取得していたが，windowsでは名前順(作成日時順？)で
取得できていたのに対し，linuxでは順番に並んでいなかった
　→ソートで解決

・類似度による特徴点の選択
　－Distanceの値によるフィルタリングの実装(閾値200程度でいい感じ)

・queryにtrainのキーポイントを重畳し，画像座標での動きを描画

・F行列について
　適当なE行列を設定し，R,Tを導出する